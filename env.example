# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=nba_stats
DB_USER=user
DB_PASSWORD=password

# Optional: BigQuery Configuration (for future use)
# GCP_PROJECT_ID=your-project-id
# BQ_DATASET=nba_analytics
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/credentials.json

# Optional: Airflow Configuration (for future use)
# AIRFLOW_HOME=/path/to/airflow
# AIRFLOW__CORE__EXECUTOR=LocalExecutor
# AIRFLOW__CORE__SQL_ALCHEMY_CONN=postgresql+psycopg2://user:password@localhost/airflow
